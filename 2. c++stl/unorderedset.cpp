//unorderedset
#include<bits/stdc++.h>
using namespace std;

int main(){
    unordered_set<int> st; //it stores unique elements and but not in sorted order;
    st.insert(1);
    st.insert(2);
    st.insert(2);
    st.insert(3);

    
    return 0;
}