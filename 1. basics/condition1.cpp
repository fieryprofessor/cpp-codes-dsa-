//if else in cpp
#include<bits/stdc++.h>
using namespace std;

int main(){
    int age;
    cin>>age;
    if(age>=18)
    cout<<"You can Vote"<<endl;
    else
    cout<<"You cant Vote before 18"<<endl;
    // more else if condtions can be given accordingly or can be nested.
    return 0;
}